enum TextSizes {small, medium, large}

enum ButtonSizes {small, medium, large}

enum OrderStatus {pending, processing, shipped, delivered, canceled}

enum PaymentMethods {cod, online, cash}

enum ColorTheme {light, dark}